字符串
问题分为两种：
1.字符串操作
2.字符串匹配

双指针

同数组使用方式


翻转
常用字符串翻转的功能，作为基础函数。
基础问题是翻转整串，翻转单词。
先看看问题是否能转换成上面两种。

KMP
获取模式的前缀表
k = next[i] 表示当前i位置的最大相同前后缀的长度。
同时也表示如果k > 0, s[k] = s[i]
KMP 的另一个核心是next回退，也就是求前缀表时需要用的。含义是：
如果当前字符串匹配失败，尝试缩小字符串范围，找到前一个字符的k，再进行比较，减少重复计算。

可以利用KMP进行一系列的推导，使用推导结果作为代码实现。




